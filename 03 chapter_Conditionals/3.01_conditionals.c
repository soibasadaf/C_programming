#include <stdio.h>

int main()
{
    // if condition
    int age = 20;
    if (age >= 18)
    {
        printf("you can caste a vote\n");
    }

    if (age % 5 == 0)
    {
        printf("the age is divisible by 5");
    }

    return 0;
}
