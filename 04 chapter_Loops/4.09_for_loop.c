#include<stdio.h>

int main(){
    int n=10,i;
    for(i=0;i<=n;i++){ //initialize,test,update
        printf("the value of i %d \n",i);
    }//in for loop fisrt the value is initialized, then it check the codion if condition is true it prints the statement, and lastly 
    //the value is updated.
    return 0;
}