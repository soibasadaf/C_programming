#include<stdio.h>
#include<math.h>

int main(){
    int a=9;
    printf("the area of square is %f",(pow(a,2)));
    
    //power function returns double datatype,
    // we need to print the output in float
    return 0;
}
